# Ktor, kotlinx.serialization, coroutines, Compose, and OkHttp publish the
# narrow consumer rules they need. Keep this file intentionally free of
# blanket app rules so R8 can optimize generated serializers and networking.
